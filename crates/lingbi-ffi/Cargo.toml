[package]
name = "lingbi-ffi"
version = "0.1.0"
edition = "2024"

[dependencies]
lingbi-application = { path = "../lingbi-application" }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread"] }

[dev-dependencies]
tempfile = "3"
